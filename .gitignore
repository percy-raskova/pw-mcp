build/

# Node-generated files
node_modules/

# Python-generated files
__pycache__/
*.py[oc]
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment variables file
.env

# Rust-generated files
debug
target

# JetBrains IDEs
.idea

# ProleWiki source data (large corpus - not versioned)
prolewiki-exports/

# ChromaDB data (keep directory structure, ignore contents)
chroma_data/*
!chroma_data/.gitkeep

# Sembr sample output (for testing)
prolewiki-sembr-sample/

# Pipeline output directories (keep directory structure, ignore contents)
extracted/*
!extracted/.gitkeep
/sembr/*
!/sembr/.gitkeep
chunks/*
!chunks/.gitkeep
embeddings/*
!embeddings/.gitkeep
logs/

# Sample pipeline: Source, extracted, chunks, and embeddings are tracked.
# ChromaDB is generated from embeddings and not tracked (too large, binary).
sample-pipeline/chroma_data/

# Model files (large binary files)
*.gguf
*.GGUF
*.safetensors
*.bin
*.pt
*.pth

# Training artifacts (generated during runs)
training_data/formatted/
training_data/*.csv
training_data/training_logs_*.json

# Runtime notebooks (but keep committed template notebooks)
training_data/DeepSeek*.ipynb
training_data/*_checkpoint*.ipynb

# Hugging Face cache
.cache/
huggingface/

# Weights & Biases
wandb/
embeddings-openai/
